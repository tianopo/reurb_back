config:
  target: http://localhost:3500
  phases:
    - duration: 30
      arrivalRate: 10
scenarios:
  - flow:
      - get:
          url: "/user"
          headers:
            Content-Type: "application/json"
